#include <stdio.h>

int main(int argc, char const *argv[])
{
    int length = 4;
    int width = 5;
    int area;

    area = length * width;

    printf("Area of retangle is: %d", area);

    return 0;
}
