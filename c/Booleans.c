#include <stdio.h>
#include <stdbool.h>

int main(int argc, char const *argv[])
{
    bool isActive = true;
    bool comparison = 10 == 5;

    printf("Active => %d \n\n", isActive);
    printf("Comparison => %d", comparison);

    return 0;
}
